#the print command, what else, prints to standard output.
print;nsp example test.

#to assemble a tasm file in an xas, use the asm command.

#test xas subscript command:
xas;nsptest1_sub

print;assemble nsp file part of example:
asm;nsptest1A
print;assemble trom part of example:
asm;nsptest1B